version: 2

models:
  - name: dim_products
    columns:
      - name: product_sk
        description: The primary key of the dim products
        tests:
          - unique
          - not_null